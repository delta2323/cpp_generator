#include <iostream>
#include "<%= classname %>.hpp"
#include "cmdline.h"

using <%= namespace.map{|n| "namespace #{n}{"}.join("::") %>::<%= classname %>

int main(int argc, char** argv){
  cmdline::parser p;
  // add options here

  if(p.parse(argc, char)){
    std::cerr << p.usage() << std::endl;
    return 0;
  }

  // write code here
  return 0;
}