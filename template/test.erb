#include <gtest/gtest.h>
#include "<%= classname %>.hpp"

<%= namespace.map{|n| "namespace #{n}{"}.join("\n") %>

TEST(<%= classname %>, trivial){
  ASSERT_TRUE(true)
}

<%= namespace.reverse.map{|n| "} // #{n}"}.join("\n") %>