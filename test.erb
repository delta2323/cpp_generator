#include <gtest/gtest.h>
#include "<%= capitalized_classname %>.hpp"

<%= namespace.map{|n| "namespace #{n}{"}.join("\n") %>

TEST(<%= upcased_classname %>, trivial){
  ASSERT_TRUE(true)
}

<%= namespace.map{|n| "} // #{n}"}.join("\n") %>